Building MNPRO

Use the autogen script to prepare the build environment.

git clone https://github.com/Black-NET/MNPRO.git

cd /root/MNPRO

chmod 755 autogen.sh

chmod 755 configure

    ./autogen.sh
    ./configure
    make

Always verify the signatures and checksums.

See doc/build-*.md for instructions on building mnprod,
the intended-for-services, no-graphical-interface, reference
implementation of MNPRO.
